# Python 3.14 / Django 6.0
Django==6.0.8
psycopg[binary]==3.3.6

# Authentication, including Google/Facebook providers
django-allauth[socialaccount]==65.19.4
argon2-cffi==25.1.0

# Payments: preserve the existing API version in settings.py
stripe==15.6.1

# Background tasks (Kombu requires redis < 6.5)
celery[redis]==5.6.3
kombu==5.6.2
billiard==4.3.0
redis==6.4.0

# Images and WSGI server
Pillow==12.3.0
gunicorn==26.2.0

# Calendar and HTTP integrations
holidays==0.105
python-dateutil==2.9.0.post0
requests==2.34.2

# PDF rendering also requires the system wkhtmltopdf executable
pdfkit==1.0.0
