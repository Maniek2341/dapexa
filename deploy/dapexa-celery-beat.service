[Unit]
Description=Dapexa Celery scheduler
After=network.target redis-server.service

[Service]
User=%APP_USER%
Group=www-data
WorkingDirectory=%APP_DIR%
EnvironmentFile=%APP_DIR%/.env
ExecStart=%APP_DIR%/.venv/bin/celery -A BusinessManager beat --loglevel=INFO
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
